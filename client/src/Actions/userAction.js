// import { getUser } from "../Api/userApi";

// export const getUserDetails = (id, token) => async dispatch => {
// 	try {
// 		const { data, status } = await getUser(id, token);
// 		if (status === 200) {
//       return dispatch()
//     }
// 	} catch (err) {
// 		console.err(err);
// 	}
// };
