# Blog Post Frontend(React App)

This app is created using React a javascript library for building user interface.

## How to run locally

fork and clone this repo, then

### Setup:

`cd client` and run `npm install` in your terminal  
now you can run it locally by using `npm start` command
